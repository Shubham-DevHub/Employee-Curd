# Minimal production requirements - Linux-friendly
# This file intentionally keeps only the packages needed to run the Django API on a Linux host (Render/Heroku/etc.).
# For local Windows development, use `requirements-dev.txt` which contains the original, full pinned list.

asgiref==3.9.1
Django==5.2.6
djangorestframework==3.16.1
django-cors-headers==4.8.0
gunicorn==23.0.0
dj-database-url==3.0.1
psycopg2-binary==2.9.11
whitenoise==6.11.0

# Optional (uncomment if deploying to MySQL):
mysqlclient==2.2.7
